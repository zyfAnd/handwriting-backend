# CloudBrush 汉字图片搜索系统 - 完整实现文档

## 🎯 系统架构

```
┌─────────────┐
│  iPhone App │
│ (CloudBrush)│
└──────┬──────┘
       │ 抓包
       ↓
┌─────────────┐     上传      ┌──────────────┐
│  mitmproxy  │ ───────────→  │ Cloudflare R2│
│ (采集图片)   │               │  (图片存储)   │
└─────────────┘               └──────┬───────┘
                                     │
                              生成映射文件
                                     │
                                     ↓
┌──────────────┐              ┌─────────────┐
│   用户浏览器  │ ←─────────→ │   Worker    │
│  (搜索界面)   │    API请求   │ (搜索API)   │
└──────────────┘              └─────────────┘
```

## 📋 实现步骤总览

1. **阶段一：数据采集** (1-2小时)
   - 设置mitmproxy抓包
   - 采集3000个汉字图片
   
2. **阶段二：数据上传** (30分钟)
   - 上传图片到Cloudflare R2
   - 生成URL映射文件
   
3. **阶段三：API开发** (1小时)
   - 部署Cloudflare Worker
   - 实现搜索API
   
4. **阶段四：前端界面** (1小时)
   - 开发搜索页面
   - 部署到Cloudflare Pages

**总时间：4小时**  
**总成本：￥2.6/月**

---

# 阶段一：数据采集

## 步骤1.1：环境准备

### 安装工具
```bash
# 安装 mitmproxy
pip install mitmproxy

# 验证安装
mitmproxy --version
```

### 准备常用汉字列表
```bash
# 创建工作目录
mkdir ~/cloudbrush-project
cd ~/cloudbrush-project

# 下载常用3500字列表
cat > common_3500_chars.txt << 'EOF'
的一是在不了有和人这中大为上个国我以要他时来用们生到作地于出就分对成会可主发年动同工也能下过子说产种面而方后多定行学法所民得经十三之进着等部度家电力里如水化高自二理起小物现实加量都两体制机当使点从业本去把性好应开它合还因由其些然前外天政四日那社义事平形相全表间样与关各重新线内数正心反你明看原又么利比或但质气第向道命此变条只没结解问意建月公无系军很情者最立代想已通并提直题党程展五果料象员革位入常文总次品式活设及管特件长求老头基资边流路级少图山统接知较将组见计别她手角期根论运农指几九区强放决西被干做必战先回则任取据处队南给色光门即保治北造百规热领七海口东导器压志世金增争济阶油思术极交受联什认六共权收证改清己美再采转更单风切打白教速花带安场身车例真务具万每目至达走积示议声报斗完类八离华名确才科张信马节话米整空元况今集温传土许步群广石记需段研界拉林律叫且究观越织装影算低持音众书布复容儿须际商非验连断深难近矿千周委素技备半办青省列习响约支般史感劳便团往酸历市克何除消构府称太准精值号率族维划选标写存候毛亲快效斯院查江型眼王按格养易置派层片始却专状育厂京识适属圆包火住调满县局照参红细引听该铁价严
EOF
```

## 步骤1.2：配置抓包脚本

将 `cloudbrush_interceptor.py` 升级为自动化版本：

```bash
# 下载增强版抓包脚本（见下文）
wget -O enhanced_collector.py <从outputs下载>

# 或直接使用我准备的脚本
```

## 步骤1.3：启动抓包

```bash
# 启动 mitmproxy
mitmweb -s enhanced_collector.py -p 8080

# 浏览器打开 http://127.0.0.1:8081
# 可以看到Web界面
```

## 步骤1.4：配置iPhone

### A. 设置代理
1. iPhone连接WiFi（与电脑同一网络）
2. 设置 → Wi-Fi → (i) → 配置代理 → 手动
   - 服务器：`你的电脑IP`（如：192.168.1.100）
   - 端口：`8080`

### B. 安装证书
1. iPhone Safari 访问：`mitm.it`
2. 点击 iOS 图标下载证书
3. 设置 → 已下载的描述文件 → 安装
4. 设置 → 通用 → 关于本机 → 证书信任设置
5. 启用 mitmproxy 证书

### C. 验证配置
```bash
# 在电脑上，mitmproxy界面应该显示iPhone的流量
# 打开iPhone Safari访问 http://example.com 测试
```

## 步骤1.5：批量采集汉字

### 方案A：手动滑动法（简单但慢）
1. 打开 CloudBrush App
2. 进入字典/字库功能
3. 按部首/笔画浏览
4. 慢慢滑动，每个字停留1秒
5. mitmproxy 自动保存图片

**预计时间**：2-3小时（3000字）

### 方案B：自动化脚本（需要越狱/Frida）
```bash
# 使用 Frida 自动触发App查询
# 需要额外配置，见附录
```

## 步骤1.6：检查采集结果

```bash
# 查看采集的文件
cd collected_characters
ls -lh

# 统计数量
ls *.png | wc -l

# 查看映射文件
cat char_url_mapping.json | jq . | head -20
```

**预期结果**：
```
collected_characters/
├── 4e00_一.png
├── 4e01_丁.png
├── 4e03_七.png
├── ...
└── char_url_mapping.json  (3000+ entries)
```

---

# 阶段二：数据上传到 Cloudflare R2

## 步骤2.1：注册 Cloudflare 账号

1. 访问：https://dash.cloudflare.com/sign-up
2. 注册免费账号
3. 验证邮箱

## 步骤2.2：创建 R2 Bucket

1. 登录 Dashboard
2. 左侧菜单 → R2
3. Create bucket
   - 名称：`chinese-characters`（全球唯一）
   - 位置：自动选择
   - 点击 Create

## 步骤2.3：生成 API Token

1. R2 → Manage R2 API Tokens
2. Create API Token
   - Token name: `character-uploader`
   - Permissions: 
     - ✅ Object Read & Write
   - TTL: Forever
3. 复制保存：
   - Access Key ID
   - Secret Access Key
   - Endpoint URL

## 步骤2.4：配置环境变量

```bash
# 在 ~/.bashrc 或 ~/.zshrc 添加
export R2_ENDPOINT='https://xxxxxxxxxxxx.r2.cloudflarestorage.com'
export R2_ACCESS_KEY_ID='your_access_key_id_here'
export R2_SECRET_ACCESS_KEY='your_secret_key_here'
export R2_PUBLIC_DOMAIN='chinese-characters.r2.dev'  # 或自定义域名

# 重新加载
source ~/.bashrc
```

## 步骤2.5：执行批量上传

```bash
# 安装依赖
pip install boto3 tqdm

# 运行上传脚本
python3 upload_to_cloud.py
```

**预期输出**：
```
============================================================
汉字图片批量上传工具
============================================================

选择服务商 (r2/s3) [默认: r2]: r2
✅ 环境变量配置完整

本地图片目录 [默认: ./collected_characters]: 
Bucket名称: chinese-characters

🚀 开始上传...
📂 找到 3000 个图片文件
☁️  上传到: R2 - chinese-characters/chars/

上传进度: 100%|████████████████| 3000/3000 [05:23<00:00, 9.28it/s]

============================================================
📊 上传完成
============================================================
总计: 3000
成功: 3000 ✅
失败: 0 ❌

💾 CDN映射已保存: cdn_url_mapping.json
   示例: 水 -> https://chinese-characters.r2.dev/chars/6c34_水.png
```

## 步骤2.6：配置公开访问

### 方案A：使用 R2.dev 域名（最简单）

1. Cloudflare Dashboard → R2
2. 选择 `chinese-characters` bucket
3. Settings → Public Access
4. 启用 `r2.dev subdomain`
5. 复制域名：`https://chinese-characters.xxxx.r2.dev`

### 方案B：自定义域名（推荐生产环境）

1. 添加域名到 Cloudflare
2. R2 Bucket → Custom Domains
3. Connect Domain: `cdn.yourdomain.com`
4. 自动创建 CNAME 记录

## 步骤2.7：验证上传

```bash
# 测试访问
curl -I https://chinese-characters.xxxx.r2.dev/chars/6c34_水.png

# 应该返回 200 OK
HTTP/2 200
content-type: image/png
...
```

---

# 阶段三：部署 Cloudflare Worker API

## 步骤3.1：安装 Wrangler CLI

```bash
# 安装 Wrangler
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 验证登录
wrangler whoami
```

## 步骤3.2：创建 Worker 项目

```bash
# 创建项目
mkdir chinese-char-api
cd chinese-char-api

# 初始化
wrangler init

# 选择：
# - Template: Hello World
# - TypeScript: No
# - Git: Yes
```

## 步骤3.3：配置 wrangler.toml

```toml
name = "chinese-char-search-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 绑定 R2 Bucket
[[r2_buckets]]
binding = "CHAR_BUCKET"
bucket_name = "chinese-characters"

# 绑定 KV（存储映射数据）
[[kv_namespaces]]
binding = "CHAR_MAPPING"
id = "YOUR_KV_NAMESPACE_ID"  # 稍后创建
```

## 步骤3.4：创建 KV Namespace

```bash
# 创建 KV
wrangler kv:namespace create "CHAR_MAPPING"

# 复制输出的 ID 到 wrangler.toml
# 例如：id = "abcd1234efgh5678"

# 上传映射数据
wrangler kv:key put --binding=CHAR_MAPPING "char_mapping" \
  --path=cdn_url_mapping.json
```

## 步骤3.5：编写 Worker 代码

见下文的完整 Worker 代码。

## 步骤3.6：部署 Worker

```bash
# 部署
wrangler deploy

# 查看部署信息
wrangler deployments list
```

**预期输出**：
```
✨ Success! Deployed chinese-char-search-api
   https://chinese-char-search-api.your-subdomain.workers.dev
```

## 步骤3.7：测试 API

```bash
# 测试搜索单个汉字
curl "https://chinese-char-search-api.your-subdomain.workers.dev/api/search?q=水"

# 预期响应
{
  "success": true,
  "query": "水",
  "results": [
    {
      "char": "水",
      "url": "https://chinese-characters.xxxx.r2.dev/chars/6c34_水.png",
      "unicode": "6c34"
    }
  ],
  "count": 1
}

# 测试搜索多个汉字
curl "https://chinese-char-search-api.your-subdomain.workers.dev/api/search?q=水火"

# 测试模糊搜索（拼音）
curl "https://chinese-char-search-api.your-subdomain.workers.dev/api/search?q=shui"
```

---

# 阶段四：前端搜索界面

## 步骤4.1：创建 Pages 项目

```bash
mkdir chinese-char-frontend
cd chinese-char-frontend

# 创建基本结构
mkdir -p public
```

## 步骤4.2：开发前端页面

见下文的完整 HTML/JS 代码。

## 步骤4.3：部署到 Cloudflare Pages

### 方案A：通过 Git（推荐）

```bash
# 初始化 Git
git init
git add .
git commit -m "Initial commit"

# 推送到 GitHub
git remote add origin https://github.com/你的用户名/chinese-char-search.git
git push -u origin main

# 在 Cloudflare Dashboard:
# Pages → Create a project → Connect to Git
# 选择你的仓库 → Deploy
```

### 方案B：直接上传

```bash
# 安装 wrangler
wrangler pages deploy public \
  --project-name=chinese-char-search
```

## 步骤4.4：配置自定义域名（可选）

1. Pages → 你的项目 → Custom domains
2. Set up a custom domain
3. 输入：`search.yourdomain.com`
4. 自动配置 DNS

---

# 完整代码文件

## 1. enhanced_collector.py (升级版抓包脚本)

见下一个文件。

## 2. Worker API (src/index.js)

见下一个文件。

## 3. 前端页面 (public/index.html)

见下一个文件。

---

# 附录

## A. 成本估算

| 项目 | 费用 |
|------|------|
| Cloudflare R2 (30MB存储) | $0 (免费额度) |
| R2 读取请求 (100万/月) | $0.36 |
| R2 流量 | $0 (完全免费) |
| Worker (100万请求/月) | $0 (免费额度) |
| Pages 托管 | $0 (免费) |
| **总计** | **$0.36/月 ≈ ￥2.6/月** |

## B. 常见问题

### Q1: 抓包看不到 HTTPS 流量？
A: 确保：
1. mitmproxy 证书已安装
2. 证书已信任（设置→证书信任设置）
3. App 没有使用证书绑定（Certificate Pinning）

### Q2: 如何绕过证书绑定？
A: 使用 Frida 或 SSL Kill Switch（需要越狱）

### Q3: 采集速度太慢？
A: 
1. 使用自动化脚本（Appium/Frida）
2. 或只采集常用1000字
3. 或使用开源数据集替代

### Q4: R2 上传失败？
A: 检查：
1. API Token 权限
2. Bucket 名称正确
3. 环境变量已设置

## C. 优化建议

### 性能优化
1. 在 Worker 中启用缓存
2. 使用 CDN 加速图片访问
3. 图片压缩（WebP格式）

### 功能扩展
1. 添加拼音搜索
2. 支持部首查询
3. 显示笔顺动画
4. 多字体支持

## D. 安全考虑

1. Worker API 添加速率限制
2. 启用 CORS 白名单
3. 使用自定义域名（避免暴露 workers.dev）
4. 定期更新 API Token

---

# 下一步

完成上述步骤后，你将拥有：

✅ 3000+ 汉字图片库  
✅ 云端存储（R2）  
✅ 搜索API（Worker）  
✅ 前端界面（Pages）  
✅ 成本：￥2.6/月  

**开始实施吧！任何问题随时问我。**
